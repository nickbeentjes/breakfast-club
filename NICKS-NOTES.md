when querying context should we have a provide context option. so give me qcntext in relation to building a recycling machine for lithium - which gets the agent focussed on pulling together a context package for the request. 

is there a context tracking aysten selssion based that tracks what contsxt has already been given - each context set sent should have a small identifier or checksum that the context engine checks if the client still has before compiling so it omits what the client has if the client does a /clear it wont gave that anymore and beeds ro be inckuded if relevant.

