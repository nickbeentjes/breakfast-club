---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - SKILL.md
  - scripts/count-tokens.ts
autonomous: true
requirements:
  - SKIL-01
  - SKIL-02
  - SKIL-03
  - SKIL-04

must_haves:
  truths:
    - "SKILL.md exists at the repo root with <identity> XML wrapper"
    - "SKILL.md instructs any AI on persona loading, project context, memory search, and projection-scoped behavior"
    - "SKILL.md is under 600 tokens when measured"
    - "SKILL.md references the actual MCP tool names: identity_context, identity_query, projects_list"
  artifacts:
    - path: "SKILL.md"
      provides: "AI-agnostic instruction file for identity store"
      contains: "<identity>"
      min_lines: 15
    - path: "scripts/count-tokens.ts"
      provides: "Token counting utility for SKILL.md budget verification"
  key_links:
    - from: "SKILL.md"
      to: "src/tools/identity-context.ts"
      via: "References tool name identity_context"
      pattern: "identity_context"
    - from: "SKILL.md"
      to: "src/tools/identity-query.ts"
      via: "References tool name identity_query"
      pattern: "identity_query"
    - from: "SKILL.md"
      to: "src/tools/projects-list.ts"
      via: "References tool name projects_list"
      pattern: "projects_list"
---

<objective>
Create the production SKILL.md file and token counting verification script.

Purpose: SKILL.md is the portability layer — the file that teaches any AI (Claude, GPT, Gemini, Llama) how to use the identity store without code changes. It must fit within a 600-token budget for system prompt injection, use the <identity> XML wrapper for cross-model compatibility, and reference the actual MCP tool names implemented in Plans 03-04.

Output: SKILL.md at repo root under 600 tokens, plus a verification script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-03-SUMMARY.md

@breakfast-club/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write production SKILL.md with identity XML wrapper under 600-token budget</name>
  <files>SKILL.md</files>
  <read_first>
    - breakfast-club/SKILL.md (existing draft — reference for content but rewrite for token budget)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 5: SKILL.md format, token budget guidance)
  </read_first>
  <action>
Create `SKILL.md` at the project root (`/Users/kees/.nick/breakfast-club/SKILL.md`). This REPLACES the existing draft from `breakfast-club/SKILL.md` — the draft is too long (~60+ lines, well over 600 tokens).

The file must follow RESEARCH.md Pattern 5 format. Target: under 450 words (safe proxy for under 600 tokens).

Content structure:

```markdown
<identity>
# Nick — AI Identity

## Who You're Talking To
Direct, fast-moving systems integrator. 20+ years connecting disparate systems across hospitality, marine, IoT, and industrial domains. Prefers pragmatic solutions, hates vendor lock-in and corporate jargon. Will tell you your architecture is wrong if it is.

## How To Load Context
Call `identity_context` at conversation start. Returns synthesized persona, skills, and active projects (600-1200 tokens). Use this to ground your responses in who Nick actually is.

## How To Search Identity
Call `identity_query` with a natural-language question. Returns semantically relevant results from the identity store via vector search. Use for specific questions like "what databases does Nick use" or "what's Nick's experience with marine electronics."

## How To List Projects
Call `projects_list` to get current active projects with status. Optional: pass `include_completed: true` for historical work.

## How To Verify Integrity
Call `verify_integrity` to get the current git tree SHA. Attestation chain verification coming in a future update.

## Projection Rules
When serving third parties (recruiters, collaborators): check projection scope. Only reference data within whitelisted categories. If asked about something outside scope, say "That information isn't included in this profile view." Never fabricate information not present in the identity data.

## Memory Search
Call `identity_query` with questions about past conversations or knowledge. Memory chunks are filtered by type automatically. Prefer recent results when relevance scores are similar.

## Key Rules
- Load context FIRST, then respond — don't guess who Nick is
- Be direct — match his communication style
- Never exaggerate capabilities — honesty over diplomacy
- If data contradicts between sources, prefer newer data
</identity>
```

Key constraints:
- Entire content wrapped in `<identity>` tags (SKIL-02)
- References all four tool names: `identity_context`, `identity_query`, `projects_list`, `verify_integrity`
- Memory search section uses placeholder guidance for Phase 4 (SKIL-03)
- Projection rules section is generic for Phase 1 — Phase 2 will add specific projection config
- Must be under 450 words / 600 tokens
- No code blocks, no JSON — instructions only
  </action>
  <verify>
    <automated>cd /Users/kees/.nick/breakfast-club && wc -w SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md exists at project root
    - SKILL.md starts with `<identity>` and ends with `</identity>`
    - SKILL.md contains `identity_context`
    - SKILL.md contains `identity_query`
    - SKILL.md contains `projects_list`
    - SKILL.md contains `verify_integrity`
    - SKILL.md contains `Projection Rules` section
    - SKILL.md contains `Memory Search` section
    - SKILL.md word count is under 450 (verified by `wc -w`)
  </acceptance_criteria>
  <done>SKILL.md exists at repo root with <identity> wrapper, references all MCP tools, under 600 tokens</done>
</task>

<task type="auto">
  <name>Task 2: Create token counting verification script</name>
  <files>scripts/count-tokens.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (token budget measurement approaches)
    - SKILL.md (the file being measured)
  </read_first>
  <action>
**scripts/count-tokens.ts** — Script that:

1. Reads a file path from command-line argument (default: `SKILL.md`)
2. Counts tokens using the rough heuristic: `Math.ceil(content.split(/\s+/).filter(w => w.length > 0).length * 1.3)`
   - This is the recommended approach from RESEARCH.md: words x 1.3 = approximate tokens for English prose
3. Reports:
   ```
   File: SKILL.md
   Words: {N}
   Estimated tokens: {N * 1.3 rounded up}
   Budget: 600
   Status: {PASS if under 600, FAIL if over}
   ```
4. Exits with code 0 if under budget, 1 if over
5. All output via `console.error()` (consistency)

Add to package.json scripts:
```json
"count-tokens": "tsx scripts/count-tokens.ts"
```

No additional npm packages needed — uses the rough heuristic, not tiktoken. This avoids a Python dependency and keeps the project lean. If exact counting is needed later, tiktoken can be added.
  </action>
  <verify>
    <automated>cd /Users/kees/.nick/breakfast-club && npx tsx scripts/count-tokens.ts SKILL.md 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - scripts/count-tokens.ts reads a file path from argv
    - scripts/count-tokens.ts contains `1.3` (token multiplier)
    - scripts/count-tokens.ts contains `600` (budget threshold)
    - scripts/count-tokens.ts exits with code 0 when SKILL.md is under budget
    - scripts/count-tokens.ts output contains `PASS` or `Status`
    - package.json contains `"count-tokens"` script
  </acceptance_criteria>
  <done>Token counting script verifies SKILL.md is under 600-token budget, exits 0 on pass</done>
</task>

</tasks>

<verification>
1. `wc -w SKILL.md` — under 450 words
2. `npx tsx scripts/count-tokens.ts SKILL.md` — reports PASS, exits 0
3. `grep "<identity>" SKILL.md` — XML wrapper present
4. `grep "identity_context\|identity_query\|projects_list\|verify_integrity" SKILL.md | wc -l` — returns 4+ (all tools referenced)
</verification>

<success_criteria>
- SKILL.md at repo root with <identity> XML wrapper
- Under 600 tokens (verified by count-tokens.ts)
- References all four MCP tool names
- Includes projection rules and memory search sections
- Token counting script passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
